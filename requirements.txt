# Runtime dependencies for hosted deployments.
# Core portfolio routes use Python standard library.
# EVCS Threat Lab endpoints require the libraries below.
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
openpyxl>=3.1.2
xgboost>=2.1.0
# Uploaded XML is untrusted. defusedxml gates it before pandas/lxml or
# ElementTree can expand entities (billion-laughs, XXE).
defusedxml>=0.7.1
